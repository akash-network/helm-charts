apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: provider-console-security-dopplersecret
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: console-token-secret
  project: provider-console-security
  config: staging
  managedSecret:
    name: provider-console-security-secret
    namespace: staging
