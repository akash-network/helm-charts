apiVersion: v1
kind: Service
metadata:
  name: {{ include "akash-rook.fullname" . }}
  labels:
    {{- include "akash-rook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "akash-rook.selectorLabels" . | nindent 4 }}
