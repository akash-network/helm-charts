apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "akash-rook.fullname" . }}-test-connection"
  labels:
    {{- include "akash-rook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "akash-rook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
