apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hostname-operator.fullname" . }}-boot
  namespace: {{ .Release.Namespace }}
data:
  run.sh: |
    #!/bin/sh
    exec /bin/akash provider hostname-operator
