apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-stats-{{ .Values.chain }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Chart.Name }}-stats-{{ .Values.chain }}
  annotations:
    secrets.doppler.com/reload: 'true'
spec:
  replicas: {{ .Values.stats.replicas | default 1 }}
  selector:
    matchLabels:
      app: {{ .Chart.Name }}-stats-{{ .Values.chain }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-stats-{{ .Values.chain }}
    spec:
      containers:
        - name: {{ .Chart.Name }}-stats-{{ .Values.chain }}
          image: ghcr.io/akash-network/console-api:{{ .Values.appVersion }}
          imagePullPolicy: "Always"
          command:
            - node
            - dist/server.js
          ports:
            - containerPort: 3000
              name: api-port
              protocol: TCP
          envFrom:
            - secretRef:
                name: {{ .Chart.Name }}-{{ .Values.chain }}-secret
            - configMapRef:
                name: {{ .Chart.Name }}-{{ .Values.chain }}-config
          env:
            - name: OTEL_SERVICE_NAME
              value: {{ .Chart.Name }}-stats-{{ .Values.chain }}
            - name: INTERFACE
              value: "rest"
          resources:
            {{- toYaml .Values.stats.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /v1/healthz/readiness
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /v1/healthz/liveness
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 3
