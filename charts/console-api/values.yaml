appVersion: "2.79.3"
hostNames:

deploymentEnv: staging
chain: sandbox

replicas: 2

resources:
  limits:
    cpu: "8"
    ephemeral-storage: "4Gi"
    memory: "16Gi"
  requests:
    cpu: "4"
    ephemeral-storage: "2Gi"
    memory: "16Gi"

backgroundJobs:
  replicas: 1
  resources:
    limits:
      cpu: "1"
      ephemeral-storage: "2Gi"
      memory: "2Gi"
    requests:
      cpu: "1"
      ephemeral-storage: "2Gi"
      memory: "2Gi"

stats:
  replicas: 1
  resources:
    limits:
      cpu: "0.5"
      ephemeral-storage: "1Gi"
      memory: "1Gi"
    requests:
      cpu: "0.5"
      ephemeral-storage: "1Gi"
      memory: "1Gi"

nodeOptions:
  enabledOTEL: false
