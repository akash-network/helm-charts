# Default values for akash-ip-operator.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: ghcr.io/ovrclk/provider-services
  tag: 0.1.0-rc1

provider_address: 
