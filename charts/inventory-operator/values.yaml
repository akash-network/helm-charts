# Default values for inventory-operator.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: ghcr.io/ovrclk/provider-services
  tag: latest
  pullPolicy: IfNotPresent
