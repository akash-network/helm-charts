apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "akash-node.fullname" . }}-test-connection"
  labels:
    {{- include "akash-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "akash-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
