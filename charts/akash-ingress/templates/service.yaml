apiVersion: v1
kind: Service
metadata:
  name: {{ include "akash-ingress.fullname" . }}
  labels:
    {{- include "akash-ingress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "akash-ingress.selectorLabels" . | nindent 4 }}
